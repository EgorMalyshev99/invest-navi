# DESIGN.md — ИнвестНавигатор

Дизайн-концепция платформы. Этот документ — источник истины по визуальному стилю, цветовой палитре, типографике, компонентам и адаптивности.

> Продуктовая логика — в [PRODUCT.md](./PRODUCT.md). Техническая архитектура — в [AGENTS.md](./AGENTS.md).

---

## Оглавление

- [Философия дизайна](#философия-дизайна)
- [Цветовая палитра](#цветовая-палитра)
- [Типографика](#типографика)
- [Компоненты](#компоненты)
- [Структура экранов](#структура-экранов)
- [Адаптивность (Responsive)](#адаптивность-responsive)
- [Семантика и доступность](#семантика-и-доступность)
- [UX-принципы](#ux-принципы)
- [AI-элементы в интерфейсе](#ai-элементы-в-интерфейсе)
- [Анимации и переходы](#анимации-и-переходы)
- [Иконки — Phosphor Icons](#иконки--phosphor-icons)
- [Spacing и Grid](#spacing-и-grid)
- [Shadows и Elevation](#shadows-и-elevation)
- [Реализация палитры в Tailwind v4](#реализация-палитры-в-tailwind-v4)
- [Связанные документы](#связанные-документы)

---

## Философия дизайна

### Главная идея

> «Личный инвестиционный помощник, который объясняет российский рынок простыми словами и помогает думать перед покупкой.»

### Визуальное позиционирование

- **Dark modern fintech** — тёмная, спокойная, уверенная палитра
- **Premium SaaS** — чистота, аккуратность, детализация
- **Clean editorial dashboard** — данные поданы как материал аналитического журнала
- **Не трейдинг-терминал** — никакого визуального давления на действие
- **Не крипто/gaming стиль** — без неоновых свечений, агрессивных анимаций, «мемных» элементов

### Принципы дизайна

| Принцип     | Описание                                                                     |
| ----------- | ---------------------------------------------------------------------------- |
| Спокойствие | Интерфейс не кричит и не подталкивает к импульсивным действиям               |
| Воздух      | Больше whitespace, меньше плотности данных на экран                          |
| Понятность  | Финансовая сложность выглядит доступно через визуальную иерархию             |
| Доверие     | Premium-ощущение: аккуратные карточки, консистентные отступы, читаемый текст |
| Доступность | Приятно и новичку, и более опытному пользователю                             |

---

## Цветовая палитра

Все цвета задаются в **hex**. Используется тёмная палитра.

### Темизация (Dark + Light)

- По умолчанию используется системная тема через `next-themes` (`defaultTheme="system"`, `enableSystem`).
- `:root` в `apps/web/app/globals.css` описывает `light` токены.
- Класс `.dark` в `apps/web/app/globals.css` описывает `dark` токены.
- Класс `.light` не используется.
- Компоненты **не** должны переключать классы вручную под каждую тему; использовать только семантические токены (`bg-background`, `text-foreground`, `border-border`, и т.д.).
- Акценты и продуктовая семантика сохраняются между темами: `primary`, `accent`, `positive`, `warning`, `negative`, `info`.

| Тема            | Основной фон | Основной текст | Surface   | Border    |
| --------------- | ------------ | -------------- | --------- | --------- |
| Light (`:root`) | `#F8FAFC`    | `#0F172A`      | `#EEF2F7` | `#CBD5E1` |
| Dark (`.dark`)  | `#080B12`    | `#F8FAFC`      | `#0F172A` | `#243044` |

### Базовые цвета

| Токен              | Hex       | Назначение                               |
| ------------------ | --------- | ---------------------------------------- |
| `background`       | `#080B12` | Основной фон приложения                  |
| `surface`          | `#0F172A` | Фон секций, sidebar, panels              |
| `surface-elevated` | `#111827` | Поднятые поверхности (dropdown, popover) |
| `card`             | `#121A2B` | Фон карточек                             |
| `border`           | `#243044` | Основная граница                         |
| `border-muted`     | `#1E293B` | Вторичная/мягкая граница                 |

### Текст

| Токен            | Hex       | Назначение                  |
| ---------------- | --------- | --------------------------- |
| `text-primary`   | `#F8FAFC` | Основной текст, заголовки   |
| `text-secondary` | `#CBD5E1` | Вторичный текст, описания   |
| `text-muted`     | `#94A3B8` | Подписи, метки, placeholder |
| `text-disabled`  | `#64748B` | Неактивные элементы         |

### Акценты

| Токен              | Hex       | Назначение                                  |
| ------------------ | --------- | ------------------------------------------- |
| `accent-primary`   | `#38BDF8` | Основной акцент (ссылки, кнопки, выделения) |
| `accent-secondary` | `#818CF8` | Вторичный акцент (категории, теги, иконки)  |
| `positive`         | `#22C55E` | Рост, успех, подтверждение                  |
| `warning`          | `#F59E0B` | Предупреждение, внимание                    |
| `negative`         | `#EF4444` | Снижение, ошибка, критическое               |
| `info`             | `#06B6D4` | Информационные элементы, подсказки          |

### Финансовые состояния

| Токен         | Hex       | Назначение                            |
| ------------- | --------- | ------------------------------------- |
| `growth`      | `#22C55E` | Рост цены, позитивная динамика        |
| `decline`     | `#EF4444` | Снижение цены, негативная динамика    |
| `neutral`     | `#94A3B8` | Без изменений / нейтральное состояние |
| `risk-medium` | `#F59E0B` | Средний уровень риска                 |
| `risk-high`   | `#F97316` | Высокий уровень риска                 |

### Маппинг на Tailwind (единая система)

В коде **один набор имён** — shadcn-токены + 7 проектных расширений. Не использовать параллельные префиксы `text-text-*`, `accent-primary` и т.п.

| Концепт (DESIGN.md)       | Tailwind-класс                | CSS var (`:root`)        |
| ------------------------- | ----------------------------- | ------------------------ |
| `background`              | `bg-background`               | `--background`           |
| `surface`                 | `bg-secondary`                | `--secondary`            |
| `surface-elevated`        | `bg-surface-elevated`         | `--surface-elevated`     |
| `card`                    | `bg-card`                     | `--card`                 |
| `border`                  | `border-border`               | `--border`               |
| `border-muted`            | `border-muted` / `bg-muted`   | `--muted`                |
| `text-primary`            | `text-foreground`             | `--foreground`           |
| `text-secondary`          | `text-secondary-foreground`   | `--secondary-foreground` |
| `text-muted`              | `text-muted-foreground`       | `--muted-foreground`     |
| `text-disabled`           | `text-disabled`               | `--disabled`             |
| `accent-primary`          | `text-primary` / `bg-primary` | `--primary`              |
| `accent-secondary`        | `text-accent` / `bg-accent`   | `--accent`               |
| `positive` / `growth`     | `text-positive`               | `--positive`             |
| `warning` / `risk-medium` | `text-warning`                | `--warning`              |
| `negative` / `decline`    | `text-negative`               | `--negative`             |
| `destructive` (ошибки UI) | `text-destructive`            | `--destructive`          |
| `info`                    | `text-info`                   | `--info`                 |
| `neutral`                 | `text-muted-foreground`       | `--muted-foreground`     |
| `risk-high`               | `text-risk-high`              | `--risk-high`            |

Источник значений: [`apps/web/app/globals.css`](apps/web/app/globals.css) (`:root` + `.dark` + `@theme inline`).

### Градиенты

| Название        | Значение                                | Применение                                  |
| --------------- | --------------------------------------- | ------------------------------------------- |
| Основной        | `from #38BDF8 to #818CF8`               | Hero-блоки, ключевые акценты, hover-эффекты |
| Background glow | `#38BDF8` → `#818CF8` при opacity 5–10% | Мягкое свечение на фоне hero-секций         |

---

## Типографика

Базовая реализация: `apps/web/src/shared/ui/typography.tsx` (`cva` + `variant` + `numeric` + `asChild`).

### Принципы типографики

- Заголовки: крупные, уверенные, `font-bold` / `font-semibold`
- Текст: читаемый (минимум 16px для body), комфортный `line-height`
- Не использовать «игровой», «криптовый» или слишком декоративный стиль
- Тон: спокойный, аналитический, понятный

### Шкала

| Элемент | Размер (Tailwind)       | Weight          | Line-height       |
| ------- | ----------------------- | --------------- | ----------------- |
| Display | `text-4xl` / `text-5xl` | `font-bold`     | `leading-tight`   |
| H1      | `text-3xl`              | `font-bold`     | `leading-tight`   |
| H2      | `text-2xl`              | `font-semibold` | `leading-snug`    |
| H3      | `text-xl`               | `font-semibold` | `leading-snug`    |
| H4      | `text-lg`               | `font-medium`   | `leading-normal`  |
| Body    | `text-base` (16px)      | `font-normal`   | `leading-relaxed` |
| Small   | `text-sm`               | `font-normal`   | `leading-normal`  |
| Caption | `text-xs`               | `font-medium`   | `leading-normal`  |

### Шрифт — Inter

**Основной шрифт:** [Inter](https://fonts.google.com/specimen/Inter)

| Свойство        | Значение                                                                  |
| --------------- | ------------------------------------------------------------------------- |
| Тип             | Sans-serif, variable font                                                 |
| Лицензия        | SIL Open Font License (бесплатный)                                        |
| Кириллица       | Полная поддержка                                                          |
| Латиница        | Полная поддержка                                                          |
| Tabular numbers | Есть (`font-variant-numeric: tabular-nums`) — все цифры одинаковой ширины |
| Subsets         | `latin`, `cyrillic`                                                       |

**Использование tabular numbers для финансовых данных:**

```css
.tabular-nums {
  font-variant-numeric: tabular-nums;
}
```

Применять `tabular-nums` на всех элементах с ценами, процентами, объёмами — чтобы цифры не «прыгали» при обновлении данных.

---

## Компоненты

### shadcn/ui — установленная библиотека

Конфигурация: `apps/web/components.json` (стиль `radix-nova`, примитивы Radix / `radix-ui`, иконки `phosphor`). Визуальный пресет Nova: умеренные скругления и сбалансированная плотность (не Lyra с `rounded-none`).
Компоненты живут в `apps/web/src/shared/ui/` (FSD-сегмент `shared`).
Кастомизация — через CSS custom properties и Tailwind-классы.

**Оглавление registry:** типографика → формы → кнопки → таблицы → раскрывающиеся секции → диалоги → навигация → карточки → обратная связь → бейджи → меню → визуализация → утилиты registry.

> В продакшене (Phase 5–6) уже задействованы компоненты из таблиц ниже. Остальные файлы в `ui/` — готовый registry shadcn для следующих экранов (дневник, портфель, графики); подключать по мере появления сценариев.

#### Типографика (Typography)

Типографика реализуется через единый компонент `Typography` в `apps/web/src/shared/ui/typography.tsx` на базе `cva`.

| API       | Описание                                                                                                         |
| --------- | ---------------------------------------------------------------------------------------------------------------- |
| `variant` | `display`, `h1`, `h2`, `h3`, `h4`, `body`, `lead`, `large`, `small`, `muted`, `blockquote`, `list`, `inlineCode` |
| `numeric` | `default` \| `tabular` (для финансовых чисел)                                                                    |
| `asChild` | Полиморфный рендер через `Slot`                                                                                  |

**Пример использования:**

```tsx
<Typography variant="h2">Картина рынка</Typography>
<Typography variant="muted">Обновлено 5 минут назад</Typography>
<Typography variant="body" numeric="tabular">+2.41%</Typography>
```

> **Правило:** не использовать сырой набор utility-классов в каждом месте отдельно. Для текстов использовать `Typography` и его `variant`-систему.

#### Формы (Forms)

**Подход:** `react-hook-form` + `zod` + shadcn Field/Form компоненты.

| Компонент      | Файл                | Назначение                                       |
| -------------- | ------------------- | ------------------------------------------------ |
| `Field`        | `field.tsx`         | Обёртка поля формы (label + input + error)       |
| `Input`        | `input.tsx`         | Текстовое поле                                   |
| `InputGroup`   | `input-group.tsx`   | Группа input с prefix/suffix                     |
| `Textarea`     | `textarea.tsx`      | Многострочный ввод                               |
| `Select`       | `select.tsx`        | Dropdown-выбор                                   |
| `Checkbox`     | `checkbox.tsx`      | Чекбокс                                          |
| `RadioGroup`   | `radio-group.tsx`   | Группа radio-кнопок                              |
| `Switch`       | `switch.tsx`        | Toggle переключатель                             |
| `Slider`       | `slider.tsx`        | Ползунок (уровень уверенности)                   |
| `Label`        | `label.tsx`         | Лейбл поля (связка с control через `htmlFor`)    |
| `Calendar`     | `calendar.tsx`      | Календарь `react-day-picker` (основа DatePicker) |
| `NativeSelect` | `native-select.tsx` | Нативный `<select>` для простых справочников     |
| `InputOTP`     | `input-otp.tsx`     | Ввод кода подтверждения (OTP), по ячейкам        |
| `Combobox`     | `combobox.tsx`      | Поиск по списку с автодополнением (registry)     |
| `DatePicker`   | composition         | `Popover` + `Button` + `Calendar`                |

**Правила форм:**

- Лейблы всегда visible (не только placeholder)
- Валидация через `zod` schema
- Ошибки отображаются под полем через `Field`
- DatePicker — композиция из `Popover` + `Calendar`, не отдельный компонент

**DatePicker — пример использования:**

```tsx
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline" data-empty={!date}>
      <CalendarBlank size={16} />
      {date ? format(date, 'PPP') : <span>Выберите дату</span>}
    </Button>
  </PopoverTrigger>
  <PopoverContent className="w-auto p-0">
    <Calendar mode="single" selected={date} onSelect={setDate} />
  </PopoverContent>
</Popover>
```

#### Кнопки и переключатели

| Компонент     | Файл               | Назначение                                               |
| ------------- | ------------------ | -------------------------------------------------------- |
| `Button`      | `button.tsx`       | Основное действие (default, outline, ghost, destructive) |
| `ButtonGroup` | `button-group.tsx` | Группа кнопок (фильтры, панель действий формы)           |
| `Toggle`      | `toggle.tsx`       | Кнопка-переключатель состояния (вкл/выкл стиль)          |
| `ToggleGroup` | `toggle-group.tsx` | Взаимоисключающий набор toggle (режимы каталога и т.п.)  |

**Правила:**

- Одна primary-кнопка на блок действий; destructive — только для необратимых операций.
- `ToggleGroup` — для переключения представления без смены страницы (например, simple/advanced каталог).
- Иконки в кнопках — Phosphor, размер согласован с `size` кнопки.

#### Таблицы (Table)

| Компонент      | Файл             | Назначение             |
| -------------- | ---------------- | ---------------------- |
| `Table`        | `table.tsx`      | Контейнер таблицы      |
| `TableHeader`  | `table.tsx`      | Шапка                  |
| `TableBody`    | `table.tsx`      | Тело                   |
| `TableRow`     | `table.tsx`      | Строка                 |
| `TableCell`    | `table.tsx`      | Ячейка                 |
| `TableCaption` | `table.tsx`      | Подпись                |
| `Pagination`   | `pagination.tsx` | Постраничная навигация |

**Правила таблиц:**

- Используются для watchlist, каталога активов, списка гипотез
- На mobile (`< 768px`) таблицы заменяются на card-list
- Мягкие `border-muted` разделители, hover-подсветка строк
- Финансовые числа в ячейках — `tabular-nums`

#### Раскрывающиеся секции (Accordion / Collapsible)

| Компонент     | Файл              | Назначение                                                |
| ------------- | ----------------- | --------------------------------------------------------- |
| `Accordion`   | `accordion.tsx`   | Вертикальные FAQ/списки с разворачиванием по секциям      |
| `Collapsible` | `collapsible.tsx` | Локальное раскрытие дополнительного контента внутри блока |

**Правила использования:**

- `Accordion` — когда есть список независимых или взаимоисключающих секций (например, FAQ по рискам, блоки «что это значит»).
- `Collapsible` — когда нужно мягко раскрыть детали **внутри** уже существующей карточки/панели, не меняя структуру экрана.
- По умолчанию контент должен быть свернут до компактного состояния, чтобы не перегружать dashboard.
- Триггеры раскрытия формулировать ясно: «Показать детали», «Что это значит», «Развернуть расчёт».

#### Диалоги и панели

| Компонент     | Файл               | Назначение                                              |
| ------------- | ------------------ | ------------------------------------------------------- |
| `Dialog`      | `dialog.tsx`       | Модальное окно (создание гипотезы, подтверждения)       |
| `Sheet`       | `sheet.tsx`        | Боковая панель (AI-помощник, детали актива)             |
| `Drawer`      | `drawer.tsx`       | Нижняя панель **на mobile** (замена Sheet/Dialog)       |
| `Popover`     | `popover.tsx`      | Всплывающий блок (подсказки к терминам)                 |
| `Tooltip`     | `tooltip.tsx`      | Краткое пояснение при hover (1–2 строки)                |
| `AlertDialog` | `alert-dialog.tsx` | Подтверждение деструктивного действия (удаление и т.п.) |
| `HoverCard`   | `hover-card.tsx`   | Расширенная карточка при наведении (превью актива)      |

**Правила:**

- Desktop: `Dialog` для модальных действий, `Sheet` для side-panel контента
- Mobile: `Drawer` вместо `Dialog`/`Sheet` где это уместно (snaps к нижней части экрана)
- `Tooltip` — для коротких пояснений (1–2 строки), появляется при hover
- `Popover` — для более объёмного контента (финансовые термины, мини-формы)

#### Навигация и Layout

| Компонент        | Файл                  | Назначение                                                |
| ---------------- | --------------------- | --------------------------------------------------------- |
| `Sidebar`        | `sidebar.tsx`         | Основная навигация (desktop: full, tablet: collapsed)     |
| `Separator`      | `separator.tsx`       | Визуальный разделитель секций                             |
| `Breadcrumb`     | `breadcrumb.tsx`      | Цепочка «Рынок → SBER» на вложенных экранах               |
| `Tabs`           | `tabs.tsx`            | Вкладки внутри экрана (обзор / риски / дневник по активу) |
| `NavigationMenu` | `navigation-menu.tsx` | Горизонтальное меню с выпадающими разделами (marketing)   |
| `ScrollArea`     | `scroll-area.tsx`     | Прокручиваемая область с кастомным скроллбаром            |
| `Resizable`      | `resizable.tsx`       | Изменяемые по ширине панели (dashboard, аналитика)        |

**Правила навигации:**

- Desktop: full sidebar с иконками + текст, collapsible
- Tablet: collapsed sidebar (только иконки)
- Mobile: bottom navigation (4–5 ключевых разделов) — кастомный компонент

#### Карточки и контейнеры

| Компонент     | Файл               | Назначение                                                    |
| ------------- | ------------------ | ------------------------------------------------------------- |
| `Card`        | `card.tsx`         | Универсальный контейнер (CardHeader, CardContent, CardFooter) |
| `Skeleton`    | `skeleton.tsx`     | Placeholder при загрузке данных                               |
| `Avatar`      | `avatar.tsx`       | Аватар пользователя                                           |
| `Empty`       | `empty.tsx`        | Пустое состояние (пустой watchlist, нет результатов поиска)   |
| `Item`        | `item.tsx`         | Строка списка с медиа/действиями (альтернатива card-list)     |
| `AspectRatio` | `aspect-ratio.tsx` | Фиксированное соотношение сторон (превью, медиа)              |

**Типы карточек (строятся на базе `Card`):**

- **Dashboard card** — метрика + краткое описание + тренд
- **Metric card** — число + label + изменение (positive/negative/neutral)
- **Insight card** — AI-пояснение с иконкой и мягким градиентным акцентом
- **Asset card** — название актива, тип, цена, изменение, risk badge
- **Alert card** — предупреждение о рисках (через `Alert` из раздела «Обратная связь»)

#### Обратная связь

| Компонент  | Файл           | Назначение                                               |
| ---------- | -------------- | -------------------------------------------------------- |
| `Sonner`   | `sonner.tsx`   | Toast-уведомления (успех, ошибка, info)                  |
| `Progress` | `progress.tsx` | Прогресс выполнения / уровень заполнения                 |
| `Alert`    | `alert.tsx`    | Статический блок: ошибка API, дисклеймер, предупреждение |
| `Spinner`  | `spinner.tsx`  | Индикатор загрузки inline (кнопка, строка таблицы)       |

**Правила:**

- Toast (`Sonner`) — короткие подтверждения действий; не дублировать длинный текст из `Alert`.
- `Alert` на экране — когда сообщение должно оставаться видимым (ошибка каталога, AI disclaimer).
- `Spinner` + `Skeleton` — spinner для действий, skeleton для layout контента.

#### Бейджи

| Компонент | Файл        | Назначение                              |
| --------- | ----------- | --------------------------------------- |
| `Badge`   | `badge.tsx` | Компактная метка (статус, тег, счётчик) |

**Проектные бейджи** (entities, на базе `Badge` / `cva`):

- **Status badge** — наблюдаю / изучаю / есть идея / в портфеле
- **Risk badge** — низкий / средний / высокий (цвет + текст, не только цвет)
- **Type badge** — акция / облигация / фонд / индекс / валюта
- **Change badge** — +2.4% / −1.1% (growth/decline/neutral, `tabular-nums`)

#### Меню и командная палитра

| Компонент      | Файл                | Назначение                                          |
| -------------- | ------------------- | --------------------------------------------------- |
| `DropdownMenu` | `dropdown-menu.tsx` | Контекстное меню действий (строка watchlist, «⋯»)   |
| `ContextMenu`  | `context-menu.tsx`  | Меню по правому клику                               |
| `Menubar`      | `menubar.tsx`       | Горизонтальная строка меню (редко, desktop-утилиты) |
| `Command`      | `command.tsx`       | Command palette / поиск по разделам и активам       |

**Правила:**

- `DropdownMenu` — основной паттерн для действий над строкой без перегрузки UI кнопками.
- `Command` — глобальный поиск (⌘K): активы, разделы; композиция с `Dialog`.

#### Визуализация данных

| Компонент | Файл        | Назначение                                           |
| --------- | ----------- | ---------------------------------------------------- |
| `Chart`   | `chart.tsx` | Обёртка Recharts + тема токенов (тренды, allocation) |

**Правила:**

- Цвета серий — из семантических токенов (`primary`, `positive`, `negative`), не произвольный hex.
- Подписи осей и легенда — `Typography` / `text-muted-foreground`.
- Для котировок на графике — `tabular-nums`.

#### Утилиты registry (по необходимости)

| Компонент   | Файл            | Назначение                                 |
| ----------- | --------------- | ------------------------------------------ |
| `Kbd`       | `kbd.tsx`       | Отображение сочетаний клавиш (⌘K, Enter)   |
| `Direction` | `direction.tsx` | Обёртка RTL/LTR при расширении `next-intl` |

> `Combobox` в формах использует `@base-ui/react` — единственное исключение в registry; остальные примитивы — `radix-ui`.

### Бизнес-компоненты (проектные)

| Компонент                | Описание                                            |
| ------------------------ | --------------------------------------------------- |
| `MarketSummaryCard`      | Hero-блок с AI-резюме состояния рынка               |
| `IndexCard`              | Мосбиржа / RGBI — значение + изменение + мини-тренд |
| `AssetRow` / `AssetCard` | Строка/карточка актива с key metrics                |
| `RiskIndicator`          | Визуальная шкала риска (low → medium → high)        |
| `HypothesisForm`         | Форма создания инвестиционной гипотезы              |
| `HypothesisCard`         | Карточка гипотезы в дневнике                        |
| `PortfolioAllocation`    | Pie/donut-chart распределения портфеля              |
| `AiInsightBlock`         | Блок AI-комментария с disclaimer                    |
| `BondExplainer`          | Визуальное объяснение параметров облигации          |
| `RiskCard`               | Карточка типа риска с объяснением и примером        |
| `TermTooltip`            | Tooltip с объяснением финансового термина           |
| `SectorStrength`         | Визуализация секторов сильнее/слабее рынка          |
| `WeeklyReview`           | Блок еженедельного AI-обзора                        |

---

## Структура экранов

### 0. Публичная зона (до входа)

Маршруты: `/` (Landing), `/login`, `/register`. Layout: **верхний header** (`PublicShell`), не sidebar.

| Элемент          | Компоненты / паттерн                                                                           |
| ---------------- | ---------------------------------------------------------------------------------------------- |
| Header           | `NavigationMenu` — лого, ссылки, `LocaleSwitcher`, `ThemeToggle`                               |
| Landing          | Секции (`<section>` + `div.container`), `Typography`, grid `Card`, CTA → Register / Login      |
| Login / Register | Центрированный `Card`, `Field` + RHF + Zod; Register шаг 2 — `RadioGroup` (уровень знаний)     |
| Темы             | Light + dark через семантические токены (`bg-background`, `bg-card`, …); пресет **radix-nova** |

**Запрещено на публичных страницах:** хардкод dark-hex (`bg-[#080B12]`, `text-white` и т.п.) — только токены.

#### Лендинг: контейнер и секции

| Правило         | Реализация                                                                                 |
| --------------- | ------------------------------------------------------------------------------------------ |
| Ширина контента | Tailwind `container` (`globals.css`: `--container-center`, `--container-padding`)          |
| Не использовать | `max-w-*` + `mx-auto` + ручной `px-*` как обёртку секции или shell                         |
| Структура       | Один смысловой блок = один компонент; корневой тег — `<section>`                           |
| Контент         | `div.container` внутри секции; фон/`py-*`/`id` — на `<section>`                            |
| Файлы           | `apps/web/src/widgets/landing-hero/ui/sections/*-section.tsx`; сборка в `landing-hero.tsx` |
| Shell           | `PublicShell` header/footer — `div.container`                                              |

`max-w-*` допустим для узких внутренних UI (модалки, popover), не для лендинга.

### Кабинет (после входа)

Маршруты: `/market`, `/watchlist`, `/profile`, … Layout: **sidebar** (`DashboardShell`), на mobile — bottom nav. Top header навигации в кабинете **не используется**. `ThemeToggle` — в footer sidebar.

### 1. Главный dashboard — «Картина рынка»

```txt
┌─────────────────────────────────────────────────────┐
│  [Hero] AI-резюме дня: "Рынок сегодня спокойный..." │
├──────────────────┬──────────────────┬───────────────┤
│  Индекс Мосбиржи │  Курс рубля      │ Ключевая ставка│
├──────────────────┴──────────────────┴───────────────┤
│  Самые заметные: растущие ↑ / падающие ↓            │
├─────────────────────────────────────────────────────┤
│  Сектора: сильнее / слабее рынка                    │
├─────────────────────────────────────────────────────┤
│  "Что это значит простыми словами"                  │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** пользователь за 5 секунд понимает: рынок спокойный, рискованный, растущий или снижающийся.

### 2. Watchlist — Список наблюдения

```txt
┌─────────────────────────────────────────────────────┐
│  Фильтры: тип | статус | период                    │
├─────────────────────────────────────────────────────┤
│  [Table / Card-list]                                │
│  Актив | Тип | Δ день | Δ неделя | Статус | Риск   │
│  ─────────────────────────────────────────────────  │
│  SBER  | Акция | +1.2% | +3.4%  | Изучаю | Средний│
│  ...                                                │
├─────────────────────────────────────────────────────┤
│  AI-подсказка: "5 из 7 активов — акции..."          │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** рабочее пространство для анализа, не список для спекуляции.

### 3. Карточка актива

```txt
┌─────────────────────────────────────────────────────┐
│  Название | Тип | Сектор                            │
├─────────────────────────────────────────────────────┤
│  Краткое описание простым языком                    │
├─────────────────────────────────────────────────────┤
│  [График-placeholder]                               │
│  Δ день | Δ неделя | Δ месяц | Δ год               │
├─────────────────────────────────────────────────────┤
│  "Что изменилось?" — AI-блок                       │
│  "Почему это может быть важно?" — AI-блок          │
│  "Какие риски?" — Risk cards                       │
│  "Вопросы перед покупкой" — Checklist              │
├─────────────────────────────────────────────────────┤
│  [+ Watchlist] [Создать гипотезу] [Сравнить]       │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** аналитический паспорт актива, не трейдинг-экран.

### 4. Инвестиционный дневник

```txt
┌─────────────────────────────────────────────────────┐
│  Мои гипотезы: все | активные | завершённые         │
├─────────────────────────────────────────────────────┤
│  [HypothesisCard]                                   │
│  Актив: SBER | Действие: Купить | Горизонт: 3 мес  │
│  Уверенность: ██████░░░░ 6/10                      │
│  Создано: 15 апр | Проверка: 15 июл                │
├─────────────────────────────────────────────────────┤
│  [+ Новая гипотеза]                                │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** фокусный, спокойный раздел. Рабочее место аналитика. Визуально особенный — это ключевой раздел платформы.

### 5. Портфель

```txt
┌─────────────────────────────────────────────────────┐
│  Общая стоимость: ₽ XXX XXX | P/L: +X.X%          │
├─────────────────────────────────────────────────────┤
│  [Donut chart] Акции / Облигации / Фонды / Валюта  │
├─────────────────────────────────────────────────────┤
│  AI-оценка структуры простым языком                 │
├─────────────────────────────────────────────────────┤
│  ⚠ Предупреждения:                                 │
│  • Высокая доля одного сектора                     │
│  • Мало защитных активов                           │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** подталкивает к осознанности, не к азарту.

### 6. Облигационный помощник

```txt
┌─────────────────────────────────────────────────────┐
│  Что такое облигация? [Expandable]                  │
├─────────────────────────────────────────────────────┤
│  Список облигаций:                                  │
│  [BondCard] Название | Доходность | Купон | Срок   │
│             Риск-шкала | Эмитент                    │
├─────────────────────────────────────────────────────┤
│  "Почему высокая доходность = высокий риск"        │
└─────────────────────────────────────────────────────┘
```

**Визуальная задача:** образовательный и спокойный тон.

### 7. AI-помощник

```txt
┌─────────────────────────────────────────────────────┐
│  [Chat / Side panel]                                │
│                                                     │
│  Suggested: "Почему акция выросла?"                 │
│             "Что такое ОФЗ?"                        │
│             "Что значит диверсификация?"             │
├─────────────────────────────────────────────────────┤
│  [User]: Почему облигации падают при росте ставки?  │
│  [AI]: Когда ставка растёт...                      │
│  ⚠ Это не инвестиционная рекомендация              │
└─────────────────────────────────────────────────────┘
```

### 8. Раздел рисков

```txt
┌─────────────────────────────────────────────────────┐
│  Типы рисков                                        │
├──────────────────┬──────────────────────────────────┤
│  [RiskCard]      │  [RiskCard]                      │
│  Рыночный риск   │  Риск ставки                     │
│  Объяснение...   │  Объяснение...                   │
│  Пример...       │  Пример...                       │
├──────────────────┼──────────────────────────────────┤
│  [RiskCard]      │  [RiskCard]                      │
│  Валютный риск   │  Дивидендный риск                │
└──────────────────┴──────────────────────────────────┘
```

---

## Адаптивность (Responsive)

### Breakpoints

| Breakpoint | Ширина           | Layout                       |
| ---------- | ---------------- | ---------------------------- |
| Mobile     | `< 768px`        | 1 column, bottom nav         |
| Tablet     | `768px – 1024px` | 2 columns, collapsed sidebar |
| Desktop    | `> 1024px`       | Multi-column, full sidebar   |

### Mobile

- Bottom navigation: Рынок / Watchlist / Дневник / Портфель / AI
- Карточки — одна колонка, полная ширина
- Таблицы → card-list (вертикальные карточки)
- Графики упрощены (мини-sparklines)
- AI summary — выше сложных данных
- Compact metric cards (2 в ряд)

### Tablet

- 2-column grid для карточек
- Sidebar collapsed (иконки), expandable
- Таблицы сохраняются с горизонтальным scroll
- AI-панель — как overlay Sheet

### Desktop

- Full sidebar с иконками + текст
- Multi-column dashboard (3–4 колонки для metric cards)
- Большие аналитические карточки
- AI-помощник — sticky side panel справа (опционально)
- Breadcrumbs + заголовок страницы

---

## Семантика и доступность

> **Принцип:** базовый разумный подход, без фанатизма. Семантика не должна вставать выше shadcn/ui компонентов. Если shadcn/ui рендерит `<div>` — не оборачивать его в `<article>` ради формальной «правильности». Компоненты библиотеки уже содержат необходимые aria-атрибуты.

### Семантическая разметка (базовый уровень)

Используем семантические теги только на уровне layout-каркаса страницы:

**Публичная зона:** `<header>` + `<main>` (marketing header).

**Кабинет:** `<aside>` (sidebar) + `<main>`; на mobile `<nav>` (bottom nav). Не смешивать marketing header с sidebar кабинета.

```html
<!-- (public) -->
<header>… NavigationMenu, ThemeToggle …</header>
<main>…</main>

<!-- (app) -->
<aside>… Sidebar …</aside>
<main>…</main>
<nav>… bottom nav (mobile) …</nav>
```

Внутри `<main>` — обычные `<div>` + shadcn/ui компоненты. Не нужно вручную добавлять `<section>`, `<article>` на каждый блок — это создаёт лишний шум без реальной пользы.

### Accessibility (базовый уровень)

| Требование     | Реализация                                                 |
| -------------- | ---------------------------------------------------------- |
| Контраст       | Палитра подобрана с учётом WCAG AA                         |
| Focus states   | Оставлять дефолтные focus-visible от shadcn/ui             |
| Labels         | `aria-label` на иконках-кнопках без текста                 |
| Не только цвет | Positive/negative сопровождаются иконкой (↑/↓) или текстом |
| Формы          | `<label>` связан с `<input>` через `htmlFor`               |
| Кнопки         | Интерактивные элементы — `<button>`, не `<div onClick>`    |

**Не делать без необходимости:** live regions, сложные aria-роли, кастомные keyboard handlers — shadcn/ui уже реализует это внутри своих компонентов (Dialog, Select, Dropdown и т.д.).

---

## UX-принципы

### Пользователь не должен чувствовать давление

- Нет агрессивных CTA «Купи сейчас»
- Нет countdown-таймеров, FOMO-элементов
- Кнопки мягкие, не кричащие: `outline` или `ghost` варианты для вторичных действий
- Primary button используется только для одного ключевого действия на экране

### Прогрессивное раскрытие сложности

- Начальный вид — простой и понятный
- Дополнительные данные — за табами, expandable-секциями, tooltip'ами
- Финансовые термины сопровождаются `TermTooltip` при первом упоминании

### Основной путь пользователя

```txt
Понять картину рынка → Добавить активы в наблюдение →
Изучить карточку актива → Создать гипотезу в дневнике →
Вернуться позже и проверить результат
```

Интерфейс визуально поддерживает этот путь: от обзора к деталям, от наблюдения к осмысленному действию.

### Первое использование

- Empty states с понятным текстом и подсказкой что делать
- Мягкий onboarding: «Начни не с покупки, а со списка наблюдения»
- Не перегружать первый экран — постепенное знакомство с функционалом

---

## AI-элементы в интерфейсе

### Визуальный стиль AI-блоков

- Мягкий градиентный border (`primary` → `accent`, opacity 20–30%)
- Иконка sparkles/brain рядом с AI-контентом
- Slightly elevated surface для выделения из обычного контента
- Disclaimer внизу: «Это не инвестиционная рекомендация»

### Типы AI-блоков

| Блок        | Контекст                                                    |
| ----------- | ----------------------------------------------------------- |
| `AiSummary` | Краткое резюме на dashboard (1–3 предложения)               |
| `AiInsight` | Объяснение в карточке актива (что изменилось, почему важно) |
| `AiComment` | Комментарий к гипотезе в дневнике                           |
| `AiChat`    | Полноценный чат в side panel                                |
| `AiReview`  | Ретроспектива гипотезы через 30/60/90 дней                  |

---

## Анимации и переходы

### Принципы анимаций

- Минимализм: анимации не должны отвлекать
- Быстрые переходы: 150–200ms для hover, 200–300ms для layout shifts
- `ease-out` для появления, `ease-in` для исчезновения
- Skeleton loading вместо спиннеров

### Где использовать

- Page transitions — subtle fade (150ms)
- Card hover — мягкий подъём тени или border highlight
- Number changes — плавная смена значений (countup)
- Sidebar collapse/expand — slide transition (200ms)
- Skeleton → content — fade transition

### Где НЕ использовать

- Никаких «bouncing» или «pulsing» кнопок
- Без flashy-анимаций на финансовых данных
- Без автоматически проигрывающихся тяжёлых анимаций

---

## Иконки — Phosphor Icons

**Библиотека:** [`@phosphor-icons/react`](https://phosphoricons.com/)

| Свойство          | Значение                                  |
| ----------------- | ----------------------------------------- |
| Пакет             | `@phosphor-icons/react`                   |
| Количество иконок | 9000+ (6 весов на каждую)                 |
| Стили             | Thin, Light, Regular, Bold, Fill, Duotone |
| Tree-shaking      | Да — в бандл попадают только используемые |
| Лицензия          | MIT                                       |

**Почему Phosphor:**

- Больше вариантов весов (6 стилей) — flexible для разных контекстов
- Duotone-стиль хорошо вписывается в premium dark dashboard
- Широкий набор финансовых и аналитических иконок
- Консистентная сетка и оптический баланс
- Простой API: `<TrendUp size={20} weight="regular" />`

**Основной стиль:** `Regular` для UI-элементов, `Duotone` для акцентов и иллюстративных иконок, `Bold` для active state в навигации.

**Размеры:**

| Токен | Размер | Применение                                  |
| ----- | ------ | ------------------------------------------- |
| sm    | `16px` | Inline с текстом, бейджи, мелкие индикаторы |
| md    | `20px` | Кнопки, table cells, card headers           |
| lg    | `24px` | Навигация, заголовки секций                 |
| xl    | `32px` | Empty states, hero-блоки                    |

**Accessibility:**

- Иконки с текстом рядом: `aria-hidden="true"`
- Standalone иконки (без текста): обязательный `aria-label`

**Пример использования:**

```tsx
import { TrendUpIcon, ChartLineUpIcon, WalletIcon, NotebookIcon } from '@phosphor-icons/react';

<TrendUpIcon size={20} weight="regular" className="text-positive" />
<ChartLineUpIcon size={24} weight="duotone" className="text-primary" />
```

---

## Spacing и Grid

### Spacing scale (Tailwind)

| Используем | Значение | Назначение                           |
| ---------- | -------- | ------------------------------------ |
| `gap-1`    | 4px      | Внутри мелких элементов              |
| `gap-2`    | 8px      | Между элементами в группе            |
| `gap-3`    | 12px     | Padding внутри compact cards         |
| `gap-4`    | 16px     | Стандартный gap между элементами     |
| `gap-6`    | 24px     | Padding внутри cards, между секциями |
| `gap-8`    | 32px     | Между крупными блоками               |
| `gap-12`   | 48px     | Между секциями страницы              |

### Grid

- Dashboard: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
- Marketing / landing / `PublicShell`: `container` (см. § Лендинг выше)
- Прочие content pages: по возможности `container`; не `max-w-* mx-auto` для основной обёртки
- Sidebar: `w-64` (desktop), `w-16` (collapsed), bottom nav (mobile)

---

## Shadows и Elevation

| Уровень | Использование             | Стиль                                   |
| ------- | ------------------------- | --------------------------------------- |
| 0       | Плоские элементы          | Без тени, только border                 |
| 1       | Cards                     | `shadow-sm` + `border border-[#243044]` |
| 2       | Elevated cards, dropdowns | `shadow-md` + `border border-[#243044]` |
| 3       | Modals, dialogs           | `shadow-lg` + backdrop blur             |

В тёмной теме тени менее заметны — акцент на borders и surface elevation (более светлый background = выше).

---

## Реализация палитры в Tailwind v4

Значения hex задаются в [`apps/web/app/globals.css`](apps/web/app/globals.css): `:root` (light) и `.dark` (dark). Переключение темы — через `next-themes` (`attribute="class"`, `defaultTheme="system"`). Мост в Tailwind — `@theme inline` (shadcn + проектные токены). Шрифт — `@theme { --font-sans }`.

```html
<div class="bg-background text-foreground border-border">
  <h1 class="text-foreground">Заголовок</h1>
  <p class="text-secondary-foreground">Описание</p>
  <span class="text-positive tabular-nums">+2.4% ↑</span>
  <span class="text-negative tabular-nums">−1.1% ↓</span>
  <span class="text-primary">Акцент</span>
</div>
```

Проектные токены без аналога в shadcn: `surface-elevated`, `disabled`, `positive`, `warning`, `negative`, `info`, `risk-high`.

---

## Связанные документы

- [PRODUCT.md](./PRODUCT.md) — бизнес-логика, разделы, сценарии
- [AGENTS.md](./AGENTS.md) — архитектура и правила разработки
- [README.md](./README.md) — технический quick start
