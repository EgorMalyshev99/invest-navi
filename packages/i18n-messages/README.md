# @repo/i18n-messages

Shared ICU MessageFormat translations used by the landing app (`next-intl`) and the
dashboard app (`react-i18next` + `i18next-icu`).

```ts
import ruMessages from '@repo/i18n-messages/ru.json';
import enMessages from '@repo/i18n-messages/en.json';
```
