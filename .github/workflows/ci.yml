name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared API types
        run: pnpm --filter @repo/api build

      - name: Check GraphQL schema drift
        run: pnpm --filter api schema:check
        env:
          DATABASE_URL: postgresql://invest:invest_secret@localhost:5432/invest_navi
          JWT_SECRET: ci-access-secret
          JWT_REFRESH_SECRET: ci-refresh-secret

      - name: Generate dashboard GraphQL types
        run: pnpm --filter dashboard codegen

      - name: Lint, typecheck, and build (affected)
        run: pnpm turbo run lint check-types build --affected
